document.querySelector('.menu-button')?.addEventListener('click',event=>{const button=event.currentTarget;const nav=document.querySelector('.nav');const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open));});
const form=document.querySelector('#contact-form');
form?.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');const status=form.querySelector('.form-status');const data=new FormData(form);button.disabled=true;button.firstChild.textContent='Sending enquiry… ';status.textContent='Sending your enquiry…';try{const response=await fetch('https://api.web3forms.com/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({access_key:'27c6bad6-6b24-435d-a2b0-5916a73bd778',subject:`TT Awards Pathway enquiry from ${data.get('name')}`,from_name:'TT Awards Pathway website',name:data.get('name'),club:data.get('club')||'Not provided',email:data.get('email'),awards:data.get('awards')||'Not selected',message:data.get('message'),botcheck:data.get('botcheck')})});const result=await response.json();if(!response.ok||!result.success)throw new Error();form.reset();status.textContent='Thank you — your enquiry has been sent successfully. We’ll be in touch soon.';}catch{status.textContent='Sorry, your enquiry could not be sent. Please try again or email ttawardspathway@gmail.com.';}finally{button.disabled=false;button.firstChild.textContent='Send enquiry ';}});
